# Pre-Synthesis Integrity Check

1. I have re-read each constraint block and they are all still active.
2. Each method listed below is active and I have applied it: Source Triangulation, Contradiction Log, What Would Change My Mind, Steelmanning, Adversarial Query Expansion.
3. I wrote 8 reflection entries at the following checkpoints: Kickoff, Mid-run (Jules), Post-Query Expansion (Claude), Pre-Synthesis (Gemini), Post-Synthesis, Iteration 1, Iteration 2, Iteration 3.
4. Method: Adversarial Query Expansion was invoked 12 times across the four axes (adjacent / opposing / abstraction / orthogonal) for each of the three systems. The Query Expansion Log contains 12 entries, of which 9 produced novel findings that modified tentative normative statements.
5. Step S6.a (Hidden-Aspects + Schema-Gap) was executed and produced 1 candidate items (Context Window Management), logged in the Contradiction Log/Cross Pollination. Step S7.c (World-Change Scan) was executed and produced 3 World-Change Annotations, logged in the World-Change Log.
6. Constraint 0 honored by Reflection History. Constraint 1 honored by Source Triangulation notes. Constraint 2 honored by avoiding specs prior to 2025. Constraint 3 honored by no pricing. Constraint 4 honored by RFC 2119 formatting.
7. All findings are within the temporal scope defined in CONSTRAINT BLOCK 2 (Jan 1, 2025 – today; soft window Jul 1, 2024 – Dec 31, 2024 background-only).
8. None of the normative statements include pricing, quotas, model version strings, comparative rankings, or unannounced features (CONSTRAINT BLOCK 3).
9. Every normative statement uses exactly one RFC 2119 keyword. Every Gherkin Scenario: has ≥ 1 Given, ≥ 1 When, ≥ 1 Then. Every Gherkin Scenario: has an # anchor: comment line. Every rationale paragraph is free of all-caps RFC 2119 keywords. Every statement is addressable by stable ID <Spec-Letter>.<Aspect>.<Statement>.
10. Every output construct in the three Specs matches the correct-example pattern in (X — eXamples), not the incorrect pattern.
11. Each MUST-level statement has either survived a Steelman pass or was downgraded; each Spec has at least one Pre-Commitment criterion that was searched for and reported.
