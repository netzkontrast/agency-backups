# Reflection

**Status:** Initialized — awaiting research execution

This directory will contain the critical thinking artifacts produced during research
execution, including:

- `friction-log.md` — **MANDATORY** — Frustration Level (FL0–FL3) declaration per
  `FRUSTRATED.md`. Must be written even if FL0.
- Individual method reflection files (e.g., `M06-audit.md`, `M07-review.md`) if the
  executing agent generates per-method reflection artifacts.

## Governing Specification

All reflection artifacts MUST conform to [FRUSTRATED.md](../../FRUSTRATED.md).
The `friction-log.md` MUST be the last artifact written before `git commit`.
