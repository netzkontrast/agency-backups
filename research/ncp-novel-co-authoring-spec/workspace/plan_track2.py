import urllib.request
import json
def search(query):
    print(f"Searching for: {query}")
search("GitHub Spec-Kit Tessl Spec-Driven Development authoring patterns failure modes agentic spec")
search("Anthropic writing specs for agents tech-spec practice Stripe GitLab Squarespace")
