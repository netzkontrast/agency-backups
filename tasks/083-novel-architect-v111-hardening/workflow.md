---
type: note
status: active
slug: novel-architect-v111-hardening-workflow
summary: "Executable implementation plan for Task 083 — 19 commits across 4 clusters (A: prose rewrite, B: scene graduation, C: linter triple, D: small fixes + retirement-placeholder verify). Generated by /sc:workflow after /sc:analyze → /sc:brainstorm → /sc:design. Includes file manifests, verification plan, cross-cluster DAG, risk register, FL pre-brief."
created: 2026-05-12
updated: 2026-05-12
---

# Workflow Plan — Task 083 (novel-architect@1.1.1-hardening)

> **Provenance.** Produced by `/sc:workflow` on 2026-05-12 from the design output of
> `/sc:analyze` (11 findings) → `/sc:brainstorm` (6 locked decisions D1–D6) → `/sc:design`
> (5 artifacts via 4 Explore subagents: sibling pattern miner, Dual-Kernel design source reader,
> line-level edit cartographer, linter-pattern miner). This document is the executable plan;
> [`task.md`](./task.md) is the canonical Task spec.

## Locked Decisions (from /sc:brainstorm)

| # | Decision | Implication |
|---|---|---|
| D1 | **Two-layer delegation contract**: orchestrator owns workflow + askuser-loop + gates; sub-modules own method application | Phase3/4/5/6 + commands rewrite into workflow-shape; sub-modules absorb method-detail |
| D2 | **No compat symlinks** under orchestrator's old `methods/{character,structure,research}/` paths — clean rewrite | Confirmed no external consumers |
| D3 | **Scene sub-module graduates in v1.1.1** | Phase 5 + 6 scene-specific detail ports into `novel-architect-scene/methods/`; SKILL.md drops "stub in v1.1.0" qualifier |
| D4 | **Linter sequencing split**: 3 of 4 linters in v1.1.1; scene-audit follows as Task 086 post-scene-graduation | Respects scene-graduation → scene-audit dependency |
| D5 | **File placeholder retirement Task now** with `task_status: blocked` until criteria (a)+(b) met | Task 084; criterion (c) already mechanically satisfied |
| D6 | **`/sc:design` spawns 4 Explore subagents** for evidence; reject 3 phantom sub-modules cartographer proposed (`-story`, `-bootstrap`, `-integration`) | Keep 4 sub-modules; drafting folds into scene; bootstrap+reflection stay in orchestrator |

## 1. Workflow Overview

| Cluster | Title | Commits | Est. session | Blocks | Closes |
|---|---|---|---|---|---|
| **A** | Prose rewrite (two-layer contract) | 9 | 3–4 h | B (scene metadata) | V111.US1 |
| **B** | Scene graduation + version bumps | 3 | 0.5 h | — | V111.US2, V111.US5 |
| **C** | Linter triple (worksheet-order, hard-rules, canon-status) | 4 | 2–3 h | — (independent) | V111.US3 |
| **D** | Retirement-placeholder verify + small fixes (M1/M2/M3) | 3 | 1 h | — | V111.US4 |
| **Total** | | **19 commits** | **6.5–8.5 h** | | 5/5 ACs |

**Single-session feasibility:** marginal. Recommended split: 2 sessions — Session 1 = A + D (prose-heavy); Session 2 = B + C (Python + metadata). Cluster A and C can run fully in parallel across two contributors / branches; merging order is A → B → C.

## 2. Per-Cluster Decomposition

### Cluster A — Prose rewrite (9 commits)

| # | Commit | Files touched | Acceptance check |
|---|---|---|---|
| A.0 | `chore(tasks): file Task 083 v1.1.1-hardening + companion Tasks 084/085/086` | `tasks/083-*/`, `tasks/084-*/`, `tasks/085-*/`, `tasks/086-*/`, `tasks/readme.md`, `TASK.md` | `tools/check-governance.sh` exit 0; all 4 Tasks pass `tools/fm/validate.py --type-check` |
| A.1 | `refactor(novel-architect): phase2 → two-layer (structure sub-module owns method detail)` | `phases/phase2-narrative-architecture.md` (REWRITE 254–307), `novel-architect-structure/methods/validation/storyform-gate-details.md` (NEW, ~117 LOC from phase2:84–200), `novel-architect-structure/methods/storyform/delegations.md` (NEW, ~53 LOC from phase2:201–253) | `grep -n "methods/character\|methods/structure/\|methods/research/" phases/phase2*.md` → 0 hits; `tools/fm/validate.py --check-body` clean |
| A.2 | `refactor(novel-architect): phase3 → two-layer (character sub-module owns method detail)` | `phases/phase3-character-architecture.md`, `novel-architect-character/methods/psycho-model-dispatch.md` (NEW), `novel-architect-character/methods/ncp-player-workflow.md` (NEW) | grep clean for phase3; pytest `render/tests/` still 74/74 |
| A.3 | `refactor(novel-architect): phase4 → two-layer (world sub-module owns method detail)` | `phases/phase4-world-research.md`, `novel-architect-world/methods/{domain-identification,research-brief-workflow,findings-integration}.md` (3 NEW files) | grep + validate.py clean |
| A.4 | `refactor(novel-architect): phase5 → two-layer (scene + structure split per D6)` | `phases/phase5-scene-matrix.md`, `novel-architect-scene/methods/structure-template-dispatch.md` (NEW), `novel-architect-structure/methods/{gate-details-scene-matrix,storyweaving-pattern}.md` (2 NEW) | grep + validate.py; **scene gains its first non-bridge method file** |
| A.5 | `refactor(novel-architect): phase6 → two-layer (drafting folds into scene per D6)` | `phases/phase6-drafting.md`, `novel-architect-scene/methods/{drafting-workflow,drafting-delegations}.md` (2 NEW) | grep + validate.py |
| A.6 | `refactor(novel-architect): commands → delegate (5 delegating: characters/design/draft/research/scenes)` | `commands/{novel-characters,novel-design,novel-draft,novel-research,novel-scenes}.md` + 5 NEW `*-command-workflow.md` files across sub-modules | `grep -rn "novel-architect-character\|-structure\|-world\|-scene" commands/` → ≥7 hits; obsolete `methods/{character,structure,research}/` refs → 0 |
| A.7 | `refactor(novel-architect): commands → REWRITE-only (start + reflect stay orchestrator-resident)` | `commands/{novel-start,novel-reflect}.md` (path-ref REWRITEs only) | grep clean |
| A.last | `chore(tasks/083): sync readme + friction-log + close cluster A` | `tasks/083-*/friction-log.md` (NEW), `tasks/readme.md` (index sync) | `tools/check-governance.sh` exit 0; `tools/fm/index_diff.py` clean |

**Cluster A acceptance:** all 12 orchestrator files purged of obsolete `methods/{character,structure,research}/` refs; sub-modules gained 14 new method files (~524 moved LOC); 287 LOC of REWRITE applied; pytest still 74/74; governance gate green.

### Cluster B — Scene graduation + version bumps (3 commits)

| # | Commit | Files touched | Acceptance check |
|---|---|---|---|
| B.1 | `feat(novel-architect-scene): graduate from stub — v1.1.1` | `skills/novel-architect-scene/SKILL.md` (drop "stub" qualifier, bump version), `skills/novel-architect/SKILL.md` (drop "stub in v1.1.0" qualifier in delegates_to, bump version) | `grep -c "stub" skills/novel-architect{,-scene}/SKILL.md` → 0; scene has ≥6 method files |
| B.2 | `fix(novel-architect): bump io_helpers.SKILL_VERSION to "1.1.1" (M1)` | `skills/novel-architect/render/io_helpers.py:258`, `skills/novel-architect/render/tests/test_io_helpers.py` (add SSoT assertion) | new test asserts `SKILL_VERSION` matches SKILL.md `metadata.version`; pytest 75/75 |
| B.3 | `docs(novel-architect): v1.1.1 changelog in learnings.md` | `skills/novel-architect/references/learnings.md` (append v1.1.1 entry below v1.1.0; reference Task 083) | manual review; entry cites Task 083 + the 5 V111.US ACs |

### Cluster C — Linter triple (4 commits)

| # | Commit | Files touched | Acceptance check |
|---|---|---|---|
| C.0 | `test(linters): storyform fixture corpus for v1.1.1 linters` | `tools/tests/fixtures/{storyform-valid,storyform-h7-violation,canon-meta-valid,canon-meta-stale}.{yaml,md}` (4 NEW) | fixtures parse via `yaml.safe_load`; one valid + one violation per linter |
| C.1 | `feat(tools): check-worksheet-order.py — WARN tier (6 rules)` | `tools/check-worksheet-order.py` (NEW), `tools/tests/test_check_worksheet_order.py` (NEW), `tools/check-governance.sh` (+1 advisory line) | linter exits 0 on `storyform-valid.yaml`, exits 2 on violation fixture; governance.sh exit 0 |
| C.2 | `feat(tools): check-hard-rules.py — WARN tier (12 rules H1-H12; uses dramatica-nav)` | `tools/check-hard-rules.py` (NEW; imports `tools/dramatica-nav/nav.py`), `tools/tests/test_check_hard_rules.py` (NEW), `tools/check-governance.sh` (+1 line) | exits 2 on H7 fixture with `…::WARN:H7.RESOLVE_MISMATCH:…`; **V111.US3 closes here** |
| C.3 | `feat(tools): check-canon-status.py — WARN tier (8 rules)` | `tools/check-canon-status.py` (NEW), `tools/tests/test_check_canon_status.py` (NEW), `tools/check-governance.sh` (+1 line) | catches reciprocity violation on stale fixture |

### Cluster D — Retirement-placeholder verify + small fixes (3 commits)

| # | Commit | Files touched | Acceptance check |
|---|---|---|---|
| D.0 | `fix(novel-architect): align io_helpers.project_workspace() docstring with implementation (M2)` | `skills/novel-architect/render/io_helpers.py:77-86` (remove unimplemented per-project YAML override promise from docstring; recommend remove rather than implement) | docstring no longer promises override behavior; pytest 75/75 |
| D.1 | `refactor(novel-architect): drop 8 unused io_helpers public helpers (M3 YAGNI)` | `skills/novel-architect/render/io_helpers.py` (remove `ensure_workspace`, `write_json`, `read_json`, `append_revision`, `write_plan_view`, `write_audit_report`, `load_project_config`, `save_project_config`) | grep clean; pytest 75/75 |
| D.2 | `chore(tasks/083, /084): close hardening + verify retirement placeholder discoverable` | `tasks/083-*/friction-log.md`, `tasks/084-*/readme.md` verify, `tasks/readme.md` | `grep -l "novel-architect-legacy" tasks/084-*/` → ≥1; **V111.US4 closes here** |

## 3. Cross-Cluster Dependency DAG

```
                ┌─ A.0 (Task filings + workflow.md + TASK.md rule) ─┐
                │                                                    │
                ▼                                                    ▼
   ┌── A.1–A.3 (phases 2/3/4) ──┐                  ┌── C.0–C.3 (linters) ──┐
   │                            │                  │  (fully independent)  │
   ▼                            ▼                  └──────────┬────────────┘
   A.4–A.5 (phases 5/6) ────────┤                             │
   (creates scene methods)      │                             │
                                ▼                             │
   ┌── A.6–A.7 (commands) ──────┤                             │
   │                            │                             │
   ▼                            ▼                             ▼
   A.last (friction + readme sync) ◄──── D.0–D.2 (small fixes) ─┐
                                │                                │
                                ▼                                │
                  B.1–B.3 (scene graduation, depends on A.4+A.5) │
                                                                  │
                                ┌─────────────────────────────────┘
                                ▼
                      PR open (single PR for entire v1.1.1)
```

**Critical path:** A.0 → A.4 → A.5 → A.6 → A.last → B.1 → B.2 → B.3 → PR (10 sequential commits).
**Parallel paths:** Cluster C runs entirely in parallel with A; D's D.0/D.1 can land any time after A.0.

## 4. Verification Plan

| AC | Scenario | Closing commit(s) | Verify command |
|---|---|---|---|
| V111.US1 | `/novel-characters` delegates to `novel-architect-character` at runtime | A.2 + A.6 | `grep -rn "methods/character\|methods/structure/\|methods/research/" skills/novel-architect/{phases,commands}/` → 0 |
| V111.US2 | Scene sub-module no longer stub | A.4 + A.5 + A.6 + B.1 | `ls skills/novel-architect-scene/methods/*.md \| wc -l` → ≥6; `grep -c "stub" skills/novel-architect-scene/SKILL.md` → 0 |
| V111.US3 | Hard-rules linter catches H7 Resolve violation | C.0 + C.2 | `python3 tools/check-hard-rules.py tools/tests/fixtures/storyform-h7-violation.yaml; echo $?` → 2; diagnostic cites `H7.RESOLVE_MISMATCH` |
| V111.US4 | Legacy retirement contract discoverable | A.0 + D.2 | `grep -l "novel-architect-legacy" tasks/084-*/` → ≥1; `task_status` = `blocked` |
| V111.US5 | `SKILL_VERSION` matches `SKILL.md.metadata.version` | B.2 | New pytest in `test_io_helpers.py` asserts equality; passes |

## 5. Sub-Task Filing Plan

### Filed at A.0 (this commit)

- **Task 083** — `novel-architect-v111-hardening` (P1, status: open, this Task)
- **Task 084** — `retire-novel-architect-legacy` (status: blocked on 083; criterion (c) already satisfied)
- **Task 085** — `promote-check-hard-rules-error-tier` (status: blocked on 083; WARN→ERROR follow-up)
- **Task 086** — `novel-architect-scene-audit-linter` (status: blocked on 083; 4th deferred linter)

## 6. Risk Register

| # | Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|---|
| R1 | **Single PR touches 12 files in cluster A** — review fatigue, merge conflicts | High | Med | Per-cluster commits keep diffs reviewable; sequence A→D→B→C; if cluster A diff > 1500 LOC, split A1 (phases) + A2 (commands) sub-PRs |
| R2 | **Two-layer documentation drift** — phase ↔ sub-module relative paths cycle dead links | High | High | Run `tools/fm/validate.py --check-body` on every cluster A commit; add a one-line `grep -rn "(\.\./){2,}novel-architect-" skills/novel-architect/ \| grep -v "^Binary"` pre-merge check |
| R3 | **Single-session compression repeats Task 070 FL2 pattern** — "lean but real" link-shell content | Med | High | Per-cluster session budget A=4h, B=0.5h, C=3h, D=1h (8.5h total → 2 sessions); commit-message convention lists `LOC moved: <N>`; stop if cluster A commit has more `TODO` lines than moved content lines |
| R4 | **Linter false positives on existing storyform corpus** — H1–H12 fire on v1.0.0-conventions files | Med | Med | WARN-tier first landing; C.0 fixture corpus is synthetic-only; Task 085 promotion gated on validation cycle |
| R5 | **Governance gate runtime regression** — 3 new linter calls add latency | Med | Low | Measure baseline before C.1; target combined regression < 500 ms; parallelize via `xargs -P 4` if regression > 1s |

## 7. Friction-Log Pre-Brief

Anticipated FL hot-spots — drafted now so the closing friction-log is honest, not retroactively reconstructed:

| Anticipated frustration | Expected FL | Honest mitigation prose |
|---|---|---|
| Cluster A scope vs. session budget | FL1–FL2 | "Cluster A's MOVE commits landed actual content (LOC counts in commit messages); REWRITE commits were path-replacement only. Per-cluster split kept review reasonable." |
| Bilingual contract drift in moved prose | FL0–FL1 | "MOVE commits preserved DE/EN distribution unchanged; spot-checked via `grep` for English markers in DE sections and vice versa. SKILL.md §Bilingual Contract referenced from each new method-file readme." |
| Linter false positive cascade | FL1 | "WARN-tier landing was deliberate (design call); fixture corpus exercised both passing and failing cases. Real-corpus violations logged as findings against the future Task 085 promotion." |
| Tooling gap on body-content shifts | FL1 | "Per-section cut-paste was scripted via `sed -n '<start>,<end>p'` + manual review; `git mv` preserved history where whole files moved." |
| Governance gate latency | FL0–FL1 | "Measured baseline at <commit before C.1>; combined regression: <Nms>. Within / over budget — mitigation: <approach>." |

**Pre-committed FL0 floor:** 19 commits + 5 ACs closed + 4 Tasks filed + governance gate green. Absent any of the above hotspots, FL0 is the honest declaration per FRUSTRATED.md (mandatory even on perfect runs).
