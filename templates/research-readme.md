---
type: research
status: active
slug: REPLACE-MUST-EQUAL-PROMPT-SLUG
summary: "REPLACE — token-cheap tl;dr of what this run produced."
created: YYYY-MM-DD
updated: YYYY-MM-DD
research_phase: kickoff
research_executes_prompt: REPLACE-PROMPT-SLUG
research_friction_level: FL0
---

# Research — REPLACE WITH SLUG

**What is this folder?** Execution workspace for the prompt at `/prompts/REPLACE-PROMPT-SLUG/`.

**Why is it here?** Per `RESEARCH.md`, every research run lives in `/research/<slug>/` where the slug equals the executing prompt's slug.

## Contents

- [`prompt.md`](./prompt.md) — Immutable run-start snapshot of the executing prompt.
- [`workspace/`](./workspace/) — Scratch notes, session.log, terminal trace.
- [`synthesis/`](./synthesis/) — Structured merge artifacts.
- [`reflection/`](./reflection/) — Critical-thinking method outputs and friction-log.
- [`output/`](./output/) — Final deliverable (`SPEC.md` or `REPORT.md`).

## Open Questions Surfaced

REPLACE — list every follow-up prompt slug filed under `/prompts/` with `prompt_kind: follow-up` and `prompt_spawned_from_research: <this-slug>`. If none, write "None".

## Workflow Assumptions

- REPLACE — document any non-obvious decision the agent made (e.g., why a method was skipped, why a track was scoped down).
