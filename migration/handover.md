---
type: note
status: active
slug: migration-handover
summary: "Next-session entry point for the 12-type ontology + auto-generated readmes refactor. ULID scope is **provisional**: L11.43 v2 (in-body) says tasks-only with per-type natural fit for 11 others; L11.43 v3 (revision-history, turn-11) extends to 6 types — re-confirm before any code lands. Read this before touching the migration."
created: 2026-05-13
updated: 2026-05-13
---

# Handover — migration in progress

**You are picking up an in-flight repo-wide refactor.** Stop here, read this document end-to-end, then read the four cross-linked files in §3 before authoring anything.

---

## 1. One-paragraph orientation

Agency is being refactored along three orthogonal axes that were ratified over Roundtables 7 and 8 of an ontology-design conversation:

1. **Artifact ontology** — the set of first-class types grows from 7 to 12 (add `role`, `lock`, `gherkin`, `friction-log`, `hook`).
2. **Placement model** — every type declares which of STANDALONE / SUBFILE / SUBDOC it permits via a per-type manifest; SUBDOC syntax is Pandoc fenced divs.
3. **Identifier convention** — **status: provisional, two-version**. L11.43 v2 (in-body) says tasks-only ULID with the other 11 types keeping per-type natural-fit identifiers (e.g. role=slug, lock=`L<round>.<sub>` notation, gherkin=`# anchor:` id). L11.43 v3 (turn-11 revision history, marked PROVISIONAL in [`locks-ratified.md §Revision history`](./locks-ratified.md#revision-history--answers-given-mid-session-after-waiver-request)) extends the ULID convention to **6 types** — task + role + lock + gherkin + friction-log + hook — reversing Decision 4. The user clicked v3 but interrupted before re-confirming the cascade implications; next session MUST re-confirm before any code or schema lands. Treat the in-body L11.43 v2 framing here as **partly stale** until that re-confirmation. Archive-first migration preserves existing names either way.

Plus one operational shift:

4. **Readme generation** — every operational `readme.md` becomes fully auto-generated by an `agency readme` CLI command, with frontmatter as the **sole** source of truth. Schemas expand to carry the prose that used to live in readme bodies.

None of this is operational yet. The locks are ratified by the user; the ADR, schema deltas, root-spec cascade, and migration tooling are all unwritten.

---

## 2. What's the immediate state of play?

**Locks ratified this session sequence:** 11 locks — see [`locks-ratified.md`](./locks-ratified.md) for the canonical list with full text.

| Lock | Subject | Status |
|---|---|---|
| L11.32‴ | 12 first-class artifact types (7 base + 5 promoted) | ratified |
| L11.36′ | Three placement modes (standalone / subfile / subdoc) | ratified |
| L11.37′ | SQLite uniform across modes | ratified |
| L11.38′ | Pandoc fenced divs as SUBDOC syntax | ratified |
| L11.39′ | Mode-aware `agency` CLI surface | ratified |
| L11.40′ | Lock placement — `decisions/locks/<lock-id>-<slug>.md` STANDALONE-only | ratified |
| L11.41′ | Hook placement — `tools/hooks/<event>/<slug>/` STANDALONE-only | ratified |
| L11.42 | Closed schemas everywhere (no `notes:` / `extra:` / `metadata:` escape hatches) | ratified |
| L11.43 (**revised**) | Tasks-only ULID convention; bare-slug folder + ULID in frontmatter; archive-first preserving NNN-slug names; slug-canonical prose with auto-disambiguation on collision | ratified |
| L11.44 (v2) | `agency readme` CLI auto-generates **every** operational readme; frontmatter is sole source of truth; schemas expand to carry narrative content | ratified |
| Decision 4 | Per-type natural-fit ID convention for the 5 promoted types — `role`→`roles/<slug>/role.md` (new top-level), `lock`→preserve `L<round>.<sub>` notation, `gherkin`/`friction-log`/`hook` deferred to natural-fit principle | partially ratified |

**Open questions blocking ADR-0013 acceptance:** see [`open-questions.md`](./open-questions.md) — Q1 through Q7.

| Q# | Subject |
|---|---|
| Q1 | Mode matrix per type (which of 12 permit STANDALONE / SUBFILE / SUBDOC) |
| Q2 | `agency promote` edge-rewrite semantics (auto / staged / per-edge confirm) |
| Q3 | Parallel-edit locking (file-level / byte-range CRDT / optimistic) |
| Q4 | Slug-collision disambiguation tooling (`agency new` time / linter / lazy) |
| Q5 | `assumption-entry` as 13th type (defer recommended) |
| Q6 | Detailed natural-fit for `gherkin` / `friction-log` / `hook` (parented vs standalone, exact paths) |
| Q7 | Sequencing — order of ADR draft / spec cascade / tooling / session close |

**Schema deltas implied but not implemented:** see [`schemas-delta.md`](./schemas-delta.md) — `type` enum gains 5 values, L1 gains `purpose` + `assumptions`, new `l2-lock.schema.json` + `l2-role.schema.json` + …, body-schema check needs to verify auto-generated readmes match renderer output.

**ADR draft:** [`adr-draft.md`](./adr-draft.md) — ADR-0013, status `Proposed`. Not yet in `decisions/` because Q1–Q7 are open and several schema deltas referenced in the Decision Outcome haven't landed.

---

## 3. Required reading before authoring

Read these in order. Each is short; together they reconstruct the design state.

1. [`next-agent-report.md`](./next-agent-report.md) — **mandatory.** Deep reflection on revision patterns, fragile decisions, inherited risks, and the eight likely failure modes if this banner is ignored.
2. [`locks-ratified.md`](./locks-ratified.md) — the 11 binding decisions. **Read §Revision history** at the bottom — L11.43 v3 (6-type scope expansion) lives there, not in the main body. The in-body L11.43 text is stale.
3. [`open-questions.md`](./open-questions.md) — what blocks promotion.
4. [`waiver.md`](./waiver.md) — authorisation to bypass `tools/check-governance.sh`. All governance revoked for the refactor window per turn 13.
5. [`schemas-delta.md`](./schemas-delta.md) — what `maintenance/schemas/` needs to gain. **Predates the Decision 4 reversal; needs revision.**
6. [`adr-draft.md`](./adr-draft.md) — the synthesis that becomes `decisions/0013-…md`. **Stale on L11.43 scope; revise before promotion.**

Then read [`session-log.md`](./session-log.md), [`original-prompt.md`](./original-prompt.md), and [`gemini-evidence.md`](./gemini-evidence.md) for full context (the why behind each lock + the eight Gemini D-citations + the verbatim user turns).

---

## 4. Where to resume

**Major directive update (turn 14):** *"All Content of the repo - including the ADRs - are revoked until further notice… Based on the Gemini Research… and all Migration artefacts - we rebuild this repo completely - starting with archiving (git mv) of every file except the Migration folder - and the .claude folder - into Archive."*

This **shifts the recommended path** away from incremental cascade (Path A) and toward **big-bang archive then rebuild**. The archive operation itself is captured as a mandatory next task in [`next-task.md`](./next-task.md) and triggers only when (a) the refactoring plan is ratified and (b) the user explicitly authorises execution.

### Recommended path (post turn-14) — Ratify, then big-bang archive, then rebuild

1. **Close Q1–Q7** in [`open-questions.md`](./open-questions.md) via AskUserQuestion. Single short session.
2. **Re-confirm turn-11 provisional answers** (R1 / R2 / R3 in [`locks-ratified.md §Revision history`](./locks-ratified.md#revision-history--answers-given-mid-session-after-waiver-request)).
3. **Revise [`adr-draft.md`](./adr-draft.md)** to reflect **whichever ULID scope step 2 confirmed** — v2 (tasks-only + per-type natural fit for the other 11 types) OR v3 (6 types with ULID, Decision 4 reversed). Revise [`schemas-delta.md`](./schemas-delta.md) to match. Earlier draft hardcoded v3; the dependency is scope-agnostic (must match user-confirmed answer at step 2), aligned with `next-task.md` P.3/P.4.
4. **User authorises archive execution.** Explicit instruction required (see [`next-task.md §2 P.5`](./next-task.md)).
5. **Execute [`next-task.md`](./next-task.md)** — `git mv` every non-exempt entry into `/archive/`. Live tree shrinks to `/migration/`, `/.claude/`, `/.git/`, `/.githooks/`, `/archive/`, plus the few user-adjudicated borderline dotfiles.
6. **Rebuild** — author the new operational tree directly into the live root, guided by `/migration/` + the Gemini research briefs. Tasks 6.x get created as they're needed.

### Why this replaces Paths A / B / C

The previous Path A (linear cascade), Path B (prototype first), and Path C (schema deltas first) all assumed **incremental modification of the existing tree**. Turn 14 explicitly rejected that:

> *"All Content of the repo - including the ADRs - are revoked until further notice"*

This is a hard reset of the design baseline. The existing 12 ADRs no longer bind. Incremental cascade is wrong because it implies keeping the pre-migration tree as a foundation; the user wants a clean rebuild from `/migration/`.

The three earlier paths are preserved below for historical reference but are **not recommended** post turn-14.

<details>
<summary>Pre-turn-14 paths (historical only — do not pursue)</summary>

#### Path A — Close Q1–Q7, ratify ADR-0013, then cascade and build (deprecated)

Linear, lowest-risk. Each question takes one AskUserQuestion round. After all 7 close, promote [`adr-draft.md`](./adr-draft.md) to `decisions/0013-twelve-type-ontology.md` with `adr_status: Proposed`, then cascade into root specs in a follow-up Task, then build the migration tooling in another follow-up Task.

#### Path B — Build a thin `agency readme` prototype first (deprecated)

Risky-by-design: scope creep is likely, but a working prototype surfaces unanticipated design questions that improve the ADR.

#### Path C — Land the schema deltas first (deprecated)

Add `lock` / `role` / `gherkin` / `friction-log` / `hook` to the L1 type enum; add `purpose` + `assumptions` to L1; author `l2-lock.schema.json`. This unblocked every other artifact in the pre-turn-14 design.

</details>

---

## 5. Operational rules while migration is in flight

- **Do not edit files inside `/migration/` casually.** Locks ratified are binding; the user has confirmed each. To revise a lock, supersede it (new lock that references `lock_supersedes: <prior-id>`) — never rewrite.
- **Do not promote anything from `/migration/` to `decisions/` or `tools/` without user authorisation.** The ADR is `Proposed`; promotion is a T3 structural action.
- **Continue using current conventions everywhere else.** `<NNN>-<slug>/` task folders, hand-written readmes, no `lock` type — current `tools/check-governance.sh` still enforces the pre-migration shape. **Existing tasks continue under the old convention.**
- **New design discussion belongs in `/migration/`.** If the user opens further design rounds (Roundtable 9, …), capture new locks here, not in `.claude/plans/`.
- **Close every session with FL declaration.** Per [AGENTS.md Closing Run Procedure](../AGENTS.md#closing-run-procedure), `friction-log.md` is mandatory — including during this migration. The migration's own friction is a first-class signal.

---

## 6. Banner contract

Two banners point agents to this folder:

- [`../CLAUDE.md`](../CLAUDE.md) — banner at top of file, above § 1.
- [`../AGENTS.md`](../AGENTS.md) — banner at top of file, above the first section.

If you remove `/migration/`, **remove both banners in the same commit**. If you add a new file here, **link it from [`readme.md`](./readme.md)'s file table**.

---

## 7. Open thread inventory (precise resume cues)

If you want a concrete "where the cursor was when the previous session ended" cue:

- **Last user message before the migration pivot:** "Drag adr and lock now - also Body content outside the markers stays hand-written (assumptions log, 'Why this folder exists' prose, etc.) - Depending this… the readme.md files should all be autogenerated - everywhere - and the frontmatter of all artifacts Need to hold all the Information needed - that should e reflection in the Schemas". This is the message that produced L11.44 v2.
- **Last user message:** "Write now Everything you did into a new /Migration folder - with all the Plans and Everything - and Create a handover for the Next Session to Continue working on the Plan… add a Note in Claude.md and agents.md at the start of the file that there is a Massive Migration and refactoring Happening". This is the directive that produced this folder.
- **Branch:** `claude/repo-refactoring-plan-CfLY5`.
- **PR (if open):** check via `mcp__github__list_pull_requests` on `netzkontrast/agency` for the branch.

---

## 8. Assumptions Log

- **Assumption A1.** Path A (close Q1–Q7 → ratify ADR → cascade → tooling) is the safest sequencing. *Status: proposed; not user-confirmed.*
- **Assumption A2.** The 11 locks ratified are not subject to re-litigation in the next session — they bind. *Status: implicit from user behavior across two roundtables; not formally declared.*
- **Assumption A3.** `/migration/` is itself a temporary workspace; it deletes when the refactor is executed. *Status: declared in [`readme.md`](./readme.md); user-confirmable.*
